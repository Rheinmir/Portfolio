version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "1050:80"
    volumes:
      - ./src:/usr/share/nginx/html
    networks:
      - portfolio-network

networks:
  portfolio-network:
    driver: bridge